apiVersion: apps/v1
kind: Deployment
metadata:
  namespace: web
  name: listee-redis
spec:
  replicas: 1
  selector:
    matchLabels:
      apps: listee-redis
  template:
    metadata:
      labels:
        app: listee-redis
    spec:
      containers:
      - name: listee-redis
        image: redis:latest
        ports:
        - containerPort: 6379
        command:
        - redis-server
        - "/config/redis.conf"
        volumeMounts:
        - mountPath: /config
          name: config
      volumes:
      - name: config
        configMap:
          name: listee-redis-configmap
      restartPolicy: Always
---
apiVersion: apps/v1
kind: Deployment
metadata:
  namespace: web
  name: listee-postgres
spec:
  replicas: 1
  selector:
    matchLabels:
      app: listee-postgres
  template:
    metadata:
      labels:
        app: listee-postgres
    spec:
      containers:
      - name: pizza-v2-postgres
        image: postgres:alpine
        ports:
        - containerPort: 5432
        envFrom:
        - configMapRef:
          name: postgres-config
        volumeMounts:
        - mountPath: /var/lib/postgresql/data
          name: postgredb
        - mountPath: /docker-entrypoint-initdb.d
          name: initialization
        - mountPath: /static
          name: backend-storage
      volumes:
      - name: postgredb
        persistentVolumeClaim:
          claimName: listee-postgres-pv-claim
      - name: initialization
        configMap:
          name: listee-postgres-init-
      restartPolicy: Always
---
apiVersion: apps/v1
kind: Deployment
metadata:
  namespace: web
  name: listee-api
spec:
  replicas: 1
  selector:
    matchLabels:
      app: listee-api
    spec:
      containers:
      - name: listee-api
        image: registry.git.rwth-aachen.de/aix-pizza-time/listee-api:2.0.1
        ports:
        - containerPort: 3000
        env:
        - name: NODE_ENV
          value: production
        - name: PORT
          value: 3000
        - name: DB_URL
          value: pizza-v2-postgres
        - name: HOST
          value: https://pizza.bartolomey.me
        imagePullSecrets:
        - name: gitlab
        restartPolicy: Always
---
# TODO add listee-web-v2 deployment